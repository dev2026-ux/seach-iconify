# Puerto del servidor
PORT=3000

# Entorno (development, production)
NODE_ENV=production

# Número de requests simultáneos que puede manejar
# Valores recomendados:
# - 10 para 50-100 usuarios (512MB RAM)
# - 20 para 100-200 usuarios (1GB RAM)
# - 50 para 300-500 usuarios (2GB RAM)
MAX_CONCURRENT=10

# Tamaño máximo del caché (número de búsquedas almacenadas)
# Valores recomendados:
# - 500 para 512MB RAM (conservador)
# - 1000 para 512MB RAM (balanceado) - DEFAULT
# - 2000 para 1GB RAM
MAX_CACHE_SIZE=1000

# Límite de memoria heap antes de reinicio automático (en MB)
# IMPORTANTE: Debe ser menor que la RAM total asignada
# Valores recomendados:
# - 400 para 512MB RAM total (conservador)
# - 450 para 512MB RAM total (balanceado) - DEFAULT
# - 900 para 1GB RAM total
# - 1800 para 2GB RAM total
MAX_MEMORY_MB=450

# Modo headless (false, 'new')
HEADLESS=new

